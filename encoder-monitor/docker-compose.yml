services:
  encoder-monitor:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "${PORT:-8090}:8090"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    restart: unless-stopped
